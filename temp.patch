*** Begin Patch
*** Update File: Assets/Scripts/Editor/TilemapToGameObjectEditor.cs
@@
-        private bool _forceWorldZ = true;
-        private bool _useTilemapZAsWorldZ = true;
-        private float _worldZOverrideValue = 0f;
-        private bool _applyZOffsetPerRow = false;
-        private float _zOffsetPerRow = 0f;
+        
@@
-            _forceWorldZ = EditorGUILayout.Toggle("월드 Z 강제값 사용", _forceWorldZ);
-            if (_forceWorldZ)
-            {
-                _useTilemapZAsWorldZ = EditorGUILayout.Toggle("Tilemap Z 사용", _useTilemapZAsWorldZ);
-                if (_useTilemapZAsWorldZ == false)
-                {
-                    _worldZOverrideValue = EditorGUILayout.FloatField("고정 Z 값", _worldZOverrideValue);
-                }
-            }
-            _applyZOffsetPerRow = EditorGUILayout.Toggle("행 기준 Z 오프셋", _applyZOffsetPerRow);
-            if (_applyZOffsetPerRow)
-            {
-                _zOffsetPerRow = EditorGUILayout.FloatField("한 행당 Z 오프셋", _zOffsetPerRow);
-            }
+            EditorGUILayout.Space();
@@
-            if (_forceWorldZ)
-            {
-                float targetZ = _useTilemapZAsWorldZ ? tilemap.transform.position.z : _worldZOverrideValue;
-                world.z = targetZ;
-            }
-
-            if (_applyZOffsetPerRow)
-            {
-                world.z += (float)(-cell.y) * _zOffsetPerRow;
-            }
-
             return world;
         }
*** End Patch
